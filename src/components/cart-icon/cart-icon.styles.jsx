import styled from "styled-components";
import tw from "twin.macro";

export const Container = styled.div`
  ${tw`cursor-pointer`}
`;
export const Count = styled.span`
  ${tw`absolute right-[0.75rem] top-[0.65rem] text-xs`}
`;
